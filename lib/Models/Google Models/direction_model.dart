

class DirectionData {
  var polylinepoints;
  var distancetext;
  var distancevalue;
  var durationvalue;
  var durationtext;

  DirectionData({this.distancevalue, this.distancetext, this.polylinepoints, this.durationvalue, this.durationtext});
}